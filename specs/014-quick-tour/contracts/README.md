# Contracts: Quick Tour

This directory is intentionally left blank. The Quick Tour feature is a purely client-side UI state tracking feature. There are no external APIs, inter-service contracts, or schema versions exposed to outside systems. All state is maintained locally in the browser's `localStorage` and managed directly by the frontend UI components.
